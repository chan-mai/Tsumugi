# ADR-0011:既定shard数を1にし分割は明示的オプトインにする

## 状況

キー単位の制御はシャードもそのキーで決めないと警告なく破綻する
shard数を1から2に増やした時点で流量制御が壊れるのが最も深刻

## 決定

既定shard数は1とし、binding内でキーは常に大域的に正しく機能する
分割時のみpartitionKeyの指定を必須にし保証が変わることを明示する

## 帰結

正しさを既定にし、性能のために正しさの一部を放棄する選択を意図的な行為にする
大半のbindingは3つ目の概念を覚えずに済む
