# ADR-0011:既定shard数を1にし分割は明示的オプトインにする

## 状況

キー単位の制御はシャードもそのキーで決めないと静かに破れる
shard数を1から2に増やした瞬間に流量制御が壊れるのが最悪

## 決定

既定shard数は1とし, binding内でキーは常に大域的に正しく効く
分割時のみpartitionKeyの指定を必須にし保証が変わることを明示する

## 帰結

正しさを既定にし,性能のために正しさの一部を手放す選択を意図的な行為にする
大半のbindingは3つ目の概念を覚えずに済む
