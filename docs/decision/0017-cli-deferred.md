# ADR-0017:専用CLIを作る方針だがv0.1では後回しにする

## 状況

リソースが多くセットアップが重い, performer追加のたびにコードと設定の2箇所へ書く構成では片方の忘れが実行時まで発覚しない

## 決定

`init`と`add-performer`を持つ専用CLIを将来作る
v0.1では作らず、起動時の設定検証で貼り付け可能な設定断片を返すことで代替する

## 帰結

CLIの価値の大半は「間違いの早期検出」のため、起動時検証だけでも導入の負担が大きく減る
