# ADR-0017:専用CLIを作る方針だがv0.1では後回しにする

## 状況

リソースが多くセットアップが重い, performer追加のたびにコードと設定の2箇所へ書く構成では片方の忘れが実行時まで分からない

## 決定

`init`と`add-performer`を持つ専用CLIを将来作る
v0.1では作らず,起動時の設定検証で貼れる設定断片を出すことで代替する

## 帰結

CLIの価値の大半は「間違いを早く教えること」なので,起動時検証だけでも摩擦がかなり減る
