# ADR-0009:流量制御を3軸(同時実行数/レート/concurrencyKey)持つ

## 状況

レート制限のある外部APIを叩くperformerに大量投入すると即死する

## 決定

binding単位に同時実行数上限,トークンバケットによるレート制限, concurrencyKey単位の同時実行上限を宣言できる

## 帰結

concurrencyKeyは単一の調停役を持つ設計でしか書けない
実測では3軸を全部有効にしても低下は約17%
