# ADR-0015: DAGはRun DOとして分離しv2で実装する

## 状況

DAGは本質的にbindingをまたぐためDO間の調整になる
D1の投影は数秒遅れる読み取りモデルであって正しさの根拠に使えない

## 決定

Runを第2のDO種別として追加し、DAGの複雑さをそこに集約する
実装はv2, v1ではjobに`run_id`/`node_id`の予約列と完了通知フックだけ用意する

## 帰結

Job DOは「完了したらrunIdに通知する」以上を知らずに済みコアの複雑さが増えない
初回マイグレーションに予約列を入れることで後から破壊的変更にならない
